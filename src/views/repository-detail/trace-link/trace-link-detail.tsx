import React, { FunctionComponent, memo } from "react";

export interface ITraceLinkDetailProps {}

const TraceLinkDetail: FunctionComponent<ITraceLinkDetailProps> = memo(
  (props: ITraceLinkDetailProps) => {
    return <div>trace link detail</div>;
  }
);

export default TraceLinkDetail;
